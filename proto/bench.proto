package bench;

enum ErrorCode {
    UNKNOWN = -1;
    OK = 0;
    ERR = 1;
}

enum ProcNumber {
    nop = 1;
    echo = 2;

    nproc = 100;
}

message NopRequest {
}

message NopReply {
    required ErrorCode eno = 1;
}

message EchoRequest {
    required bytes message = 1;
}

message EchoReply {
    required ErrorCode eno = 1;
    required bytes message = 2;
}
